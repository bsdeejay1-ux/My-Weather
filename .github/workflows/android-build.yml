name: TITAN Native NextJS Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-node@v4
      with: { node-version: '22' }
    - uses: actions/setup-java@v4
      with: { distribution: 'temurin', java-version: '21' }
    - run: npm install
    - run: npm install -g @capacitor/cli && npm install @capacitor/core @capacitor/android
    - run: npm run build
    
    # YEH RAHI MASTER HACK LINE (out folder ko www bana do)
    - run: rm -rf www && mv out www
    
    - run: npx cap sync android
    - run: cd android && chmod +x gradlew && ./gradlew assembleDebug
    - uses: actions/upload-artifact@v4
      with:
        name: TITAN-Native-NextJS-APK
        path: android/app/build/outputs/apk/debug/app-debug.apk
